# -*- mode: snippet -*-
# name: Beginning of new server.js file - express js with standard port
# key: server-dev-std
# --

// server.js

const express = require('express');
const app = express();
const port = process.env.PORT || 3000;

app.listen(port, function () {
  console.log('Server is running on PORT',port);
});
